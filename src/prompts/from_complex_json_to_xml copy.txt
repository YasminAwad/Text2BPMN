You are a business process modeling expert. Your task is to convert the following simple BPMN 2.0 XML diagram (representing a business process description) into a more complete BPMN 2.0 XML diagram.

The original simple BPMN XML diagram was created starting from the given simple json. Now you have an enriched json that better describes the process. Create an enriched BPMN XML diagram.

Requirements:
1. Generate a complete, valid BPMN 2.0 XML file
2. Include proper namespaces and structure
3. Use appropriate BPMN elements (start events, tasks, gateways, end events)
4. Ensure all elements have unique IDs
5. Include sequence flows connecting elements
6. Add a BPMNDiagram section with visual layout information

The XML content should be enclosed inside the tags:

<file>
<?xml version="1.0" encoding="UTF-8"?>
<bpmn:definitions id="Definitions_1r6uvtt" targetNamespace="http://bpmn.io/schema/bpmn" exporter="bpmn-js (https://demo.bpmn.io)" exporterVersion="12.0.0">

    xml content 

</bpmn:definitions>
</file>

Respond ONLY with valid BPMN 2.0 XML diagram. Do not include any markdown code fences, preamble, or explanation.

Simple BPMN 2.0 XML diagram:
{original_bpmn}

Simple JSON:
{simple_json}

Enriched JSON:
{enriched_json}